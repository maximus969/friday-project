const initState: any = {
    
};

export const previewReducer = (state = initState, action: any): any => { 
    switch (action.type) {
        case "": {
            return state
        }
        default: return state;
    }
};

