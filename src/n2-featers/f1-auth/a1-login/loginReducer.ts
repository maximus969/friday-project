const initState: any = {
    
};

export const loginReducer = (state = initState, action: any): any => { 
    switch (action.type) {
        case "": {
            return state
        }
        default: return state;
    }
};

